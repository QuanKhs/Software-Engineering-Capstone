import { useRef } from "react";
import image from "../../../../common/image/avatar4.png";
import * as SC from "./style";

const SizeBar = (props) => {
  return <SC.Container></SC.Container>;
};

export default SizeBar;
